/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.williamloughran.co.uk', 'cdn.motor1.com', 'upload.wikimedia.org', 'yourdomain.com'], // Add all external domains here
    },
};

export default nextConfig;
